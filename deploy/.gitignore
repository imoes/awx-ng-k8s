# Secrets + lokale Overrides — niemals einchecken
.env
# Lokale docker-compose-Overrides (Proxy, lokale Ports, Debug-Volumes …)
docker-compose.override.yml
secrets/pg_password
secrets/secret_key

# Laufzeit-Daten
data/postgres/
data/redis/
data/receptor/
data/projects/

# Collections-Tarballs sind absichtlich eingecheckt (für Offline-Build ohne Galaxy)
# collections/
